[build]
  base = "apps/backoffice"
  command = "cd ../.. && npm install && npx turbo run build --filter=@restaurant/backoffice && cd apps/backoffice && ln -sf ../../node_modules node_modules 2>/dev/null || true"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"

# Next.js plugin handles routing automatically
[[plugins]]
  package = "@netlify/plugin-nextjs"

