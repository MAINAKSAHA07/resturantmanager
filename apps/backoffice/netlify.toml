[build]
  base = "apps/backoffice"
  command = "cd ../.. && npm install && npm run build --filter=@restaurant/backoffice"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"

# Next.js plugin handles routing automatically
[[plugins]]
  package = "@netlify/plugin-nextjs"

